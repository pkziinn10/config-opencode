---
description: Show caveman session token-savings stats (+lifetime with --all or --since Nd)
argument-hint: "[--all] [--since Nd]"
---

Stats are computed and displayed by the plugin hook. Output is injected directly — the model does not read files or aggregate data.
